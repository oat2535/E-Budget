# Shell scripts run inside Linux containers — a CRLF shebang breaks them
# with "bad interpreter" errors, so force LF regardless of local git config.
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
